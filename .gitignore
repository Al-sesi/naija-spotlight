# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Deployment caches
.vercel
.output
.turbo

# Supabase CLI binary (100MB+ - exceeds Vercel limits; use npx instead)
# NOTE: The project-level `supabase/` directory (migrations, functions, config.toml)
#       is intentionally NOT ignored — it's tracked in git.
/supabase.exe
supabase/.temp
.env*.local
